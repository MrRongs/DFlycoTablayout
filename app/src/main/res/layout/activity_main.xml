<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <android.support.v4.view.ViewPager
        android:id="@+id/viewPager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="52dp" />

    <FrameLayout
        android:id="@+id/fl_layout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:layout_alignParentBottom="true"
        android:background="@color/white">

        <VideoView
            android:layout_width="match_parent"
            android:layout_height="0.01dp"
            android:background="@color/color_999999" />

        <com.rong.flycotablayout.CommonTabLayout
            android:id="@+id/tab"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:tl_indicator_color="@color/white"
            app:tl_indicator_height="0dp"
            app:tl_indicator_width="0dp"
            app:tl_tab_space_equal="true"
            app:tl_indicator_anim_start="true"
            app:tl_textBold="BOTH"
            app:tl_textSelectColor="@color/color_14ced0"
            app:tl_textUnselectColor="@color/color_b3b3b3"
            app:tl_textsize="10sp" />
    </RelativeLayout>
</FrameLayout>

</RelativeLayout>